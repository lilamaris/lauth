package com.lilamaris.lauth.identity.jdbc.sql;

public class ScopeSql {
    public static final String INSERT = """
            INSERT INTO scope (
            
            ) VALUES (
            
            )
            """;
}
